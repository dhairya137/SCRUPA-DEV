# Project 

 

### Usage

*Requirements*

* pip
* virtualenv
* PostgreSQL

To setup your system, run:
`./setup.sh`

To run with docker 
`docker-compose up` 

To run the tests, run:
`pytest`

To run the app, run:
`python3 run.py`


 
